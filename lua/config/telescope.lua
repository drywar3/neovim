local builtin = require 'telescope.builtin'

nset('<leader>ff', builtin.find_files)
nset('<leader>fg', builtin.live_grep)
nset('<leader>cm', builtin.commands)
nset('<leader>ch', builtin.command_history)
nset('<leader>cl', builtin.colorscheme)
nset('<leader>fb', builtin.oldfiles)
